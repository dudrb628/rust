fn main() {
    
}

// fn plus_one(x: Option<i32>) -> Option<i32>{
    //     match x {
        //         Some(i) => Some(i+1),
        //     }
        // }
        
// None 값에 해당하는 경우를 처리하지 않기 때문에 버그가 있고 컴파일조차 되지 않는다.
// match는 반드시 모든 경우를 처리해야 한다.